<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Appenders>
        <!-- Console Appender for application errors only -->
        <Console name="Console" target="SYSTEM_OUT">
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- Async Console -->
        <Async name="ConsoleAsync">
            <AppenderRef ref="Console"/>
        </Async>

        <!-- Rolling File Appender for application logs (JSON) -->
        <RollingFile name="AppFile" fileName="logs/app.log"
                     filePattern="logs/app-%d{yyyy-MM-dd}.log.gz">
            <JsonLayout compact="true" eventEol="true"
                        includeStacktrace="true"
                        includeTimeMillis="true"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
        </RollingFile>
        <Async name="AppFileAsync">
            <AppenderRef ref="AppFile"/>
        </Async>

        <!-- Rolling File Appender for HTTP logs (JSON) -->
        <RollingFile name="HttpFile" fileName="logs/http.log"
                     filePattern="logs/http-%d{yyyy-MM-dd}.log.gz">
            <JsonLayout compact="true" eventEol="true"
                        includeStacktrace="true"
                        includeTimeMillis="true"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
        </RollingFile>
        <Async name="HttpFileAsync">
            <AppenderRef ref="HttpFile"/>
        </Async>
    </Appenders>

    <Loggers>
        <!-- Application logs -->
        <Logger name="com.example.retailplatform" level="DEBUG" additivity="false">
            <AppenderRef ref="AppFileAsync"/>
            <AppenderRef ref="ConsoleAsync"/>
        </Logger>

        <!-- HTTP logs -->
        <Logger name="HTTP_LOG" level="INFO" additivity="false">
            <AppenderRef ref="HttpFileAsync"/>
        </Logger>

        <!-- Root logger -->
        <Root level="INFO">
            <AppenderRef ref="AppFileAsync"/>
            <AppenderRef ref="ConsoleAsync"/>
        </Root>
    </Loggers>
</Configuration>
