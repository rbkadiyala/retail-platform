apiVersion: 1
providers:
  - name: 'User Service Dashboards'
    orgId: 1
    folder: 'User Service'
    folderUid: 'user-service'
    type: file
    options:
      path: /var/lib/grafana/dashboards/user-service
      updateIntervalSeconds: 10
